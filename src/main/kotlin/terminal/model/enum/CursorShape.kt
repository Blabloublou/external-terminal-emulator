package terminal.model.enum

/**
 * Shape of the cursor when displayed.
 */
enum class CursorShape {
    /** Full cell. */
    Block,
    /** Underline under the cell. */
    Underline,
    /** Vertical bar at the left of the cell. */
    Bar,
}
